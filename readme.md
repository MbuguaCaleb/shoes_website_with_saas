**New Things**

```
(a)font size:clamp() helps in responsive titles design.

(b)@supports(property:value){

    Tests your browser support for certain features.

    }

Example

@supports (display: grid) {
  .main {
    display: grid;
  }
}

(c) Flex wrap(Helps you arrange your items vertically if they do not all fit horizontally)

(d)Pseudo elemets like before and after

woow..puts some content right after or before an element.

(e)Z-index .Its more like send to back.

(f)I shall have a look at Stacking context in CSS.When having a look at the Z-index.



```
